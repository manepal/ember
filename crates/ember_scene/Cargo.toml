[package]
name = "ember_scene"
version = "0.1.0"
edition = "2021"
description = "Scene graph, RON serialization, prefabs"

[dependencies]
ember_core = { path = "../ember_core" }
serde = { version = "1.0", features = ["derive"] }
ron = "0.8"

[dev-dependencies]
